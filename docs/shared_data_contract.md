# Shared Data Contract

Status: **Checkpoint 0 draft**

This contract is the read-only input for the three specialist agents. No agent may add a shared key, table, endpoint, or data source beyond this contract without an owner checkpoint.

## Lineage Spine

```text
experiment --< run --< checkpoint --< model_version --< eval_run --< eval_output
                |              |
                |              +-- promoted to model_version
       consumes dataset_version
                |
   emits training_metrics + compute_metrics keyed by run_id and node_id

model_version --< deployment --< inference_traffic   optional later phase
eval_failure --> saved as dataset_candidate --> dataset_version closes loop
```

## Canonical Keys

Use these exact column names everywhere:

- `dataset_id`
- `dataset_version_id`
- `run_id`
- `run_config_id`
- `checkpoint_id`
- `model_id`
- `model_version_id`
- `node_id`
- `gpu_id`
- `eval_suite_id`
- `eval_run_id`
- `eval_case_id`
- `eval_output_id`
- `eval_failure_id`
- `regression_id`
- `user_id`

## Shared Conventions

- Timestamps are UTC ISO-8601.
- Event time column is `timestamp`; lifecycle columns may use `created_at`, `started_at`, and `ended_at`.
- All foreign keys are explicit columns with canonical names.
- Dataset versions, model versions, and eval-suite versions are immutable.
- Analytical time-series tables use long format: `entity_id`, `timestamp`, `step`, `metric_name`, `metric_value`.
- Analytical data lives in Parquet + DuckDB.
- App metadata lives in Postgres.
- Raw records and artifacts live in local object-store-style folders.
- Data source labels are required for provenance:
  - `PUBLIC_REAL`: real public large-size downloaded data
  - `GENERATED_REAL`: real data generated by local pipelines, quality jobs, model runs, or eval runs
  - `SYNTHETIC_REALISTIC`: constrained synthetic data used only when public/generated real data is unavailable, too expensive, or too slow

## Shared Status Enums

### Run Status

`queued`, `running`, `failed`, `completed`, `killed`

### Checkpoint Status

`created`, `validated`, `promoted`, `rejected`

### Model Version Status

`candidate`, `registered`, `promoted`, `archived`

### Eval Run Status

`queued`, `running`, `failed`, `completed`, `cancelled`

### Dataset Version Status

`draft`, `validated`, `published`, `deprecated`

## Core Tables

The table definitions below intentionally define shared boundaries, not every future UI field. Agent-specific additions require an owner checkpoint if they affect another agent or the shared contract.

### App Metadata: Postgres

#### `experiments`

- `experiment_id`
- `name`
- `description`
- `created_at`
- `created_by_user_id`

#### `dataset_versions`

- `dataset_id`
- `dataset_version_id`
- `name`
- `version`
- `status`
- `source_priority`
- `raw_uri`
- `parquet_uri`
- `schema_uri`
- `parent_dataset_version_id`
- `created_at`
- `created_by_user_id`

#### `datasets`

- `dataset_id`
- `name`
- `description`
- `category`
- `default_task_type`
- `source_label`
- `owner_user_id`
- `created_at`
- `updated_at`

#### `dataset_ingestion_jobs`

- `dataset_ingestion_job_id`
- `dataset_id`
- `source_dataset_name`
- `source_config_json`
- `status`
- `started_at`
- `ended_at`
- `records_seen`
- `records_written`
- `error_message`
- `created_by_user_id`

#### `run_configs`

- `run_config_id`
- `dataset_version_id`
- `model_id`
- `config_uri`
- `created_at`
- `created_by_user_id`

#### `training_runs`

- `run_id`
- `experiment_id`
- `run_config_id`
- `dataset_version_id`
- `status`
- `started_at`
- `ended_at`
- `created_by_user_id`

#### `checkpoints`

- `checkpoint_id`
- `run_id`
- `dataset_version_id`
- `step`
- `status`
- `artifact_uri`
- `created_at`

#### `model_versions`

- `model_id`
- `model_version_id`
- `checkpoint_id`
- `run_id`
- `dataset_id`
- `dataset_version_id`
- `status`
- `artifact_uri`
- `created_at`
- `created_by_user_id`

#### `eval_suites`

- `eval_suite_id`
- `name`
- `version`
- `status`
- `case_source_uri`
- `created_at`

#### `eval_runs`

- `eval_run_id`
- `eval_suite_id`
- `model_version_id`
- `run_id`
- `checkpoint_id`
- `dataset_version_id`
- `status`
- `started_at`
- `ended_at`

#### `eval_failures`

- `eval_failure_id`
- `eval_run_id`
- `eval_output_id`
- `eval_case_id`
- `model_version_id`
- `dataset_version_id`
- `severity`
- `failure_type`
- `created_at`

#### `dataset_candidates`

- `dataset_candidate_id`
- `eval_failure_id`
- `source_eval_run_id`
- `source_model_version_id`
- `target_dataset_id`
- `status`
- `created_at`
- `created_by_user_id`

### Analytical: Parquet + DuckDB

#### `training_metrics`

Long-format time-series keyed by `run_id`.

- `run_id`
- `timestamp`
- `step`
- `metric_name`
- `metric_value`

#### `compute_metrics`

Long-format time-series keyed by `run_id`, `node_id`, and optional `gpu_id`.

- `run_id`
- `node_id`
- `gpu_id`
- `timestamp`
- `step`
- `metric_name`
- `metric_value`

#### `eval_outputs`

- `eval_output_id`
- `eval_run_id`
- `eval_case_id`
- `model_version_id`
- `dataset_version_id`
- `output_text`
- `score`
- `scoring_method`
- `created_at`

#### `dataset_quality_reports`

- `dataset_version_id`
- `timestamp`
- `metric_name`
- `metric_value`
- `source_priority`

#### `dataset_records`

- `record_id`
- `dataset_id`
- `dataset_version_id`
- `source_dataset_name`
- `source_split`
- `source_row_id`
- `category`
- `task_type`
- `input_text`
- `instruction`
- `context`
- `question`
- `chosen_text`
- `rejected_text`
- `target_text`
- `response_text`
- `prompt_messages_json`
- `metadata_json`
- `content_hash`
- `source_label`
- `created_at`

#### `dataset_schema_profiles`

- `dataset_version_id`
- `field_name`
- `field_type`
- `non_null_count`
- `null_count`
- `empty_count`
- `distinct_count`
- `min_length`
- `max_length`
- `mean_length`
- `example_values_json`
- `created_at`

#### `dataset_duplicate_reports`

- `dataset_version_id`
- `content_hash`
- `duplicate_count`
- `record_ids_json`
- `created_at`

#### `dataset_pii_scan_results`

- `dataset_version_id`
- `record_id`
- `pii_type`
- `match_count`
- `scanner_name`
- `scanner_version`
- `created_at`

#### `dataset_token_statistics`

- `dataset_version_id`
- `record_id`
- `tokenizer_name`
- `input_token_count`
- `target_token_count`
- `total_token_count`
- `created_at`

#### `dataset_lineage`

- `dataset_id`
- `source_dataset_version_id`
- `target_dataset_version_id`
- `lineage_event_type`
- `transform_name`
- `transform_config_uri`
- `created_at`
- `created_by_user_id`

#### `dataset_usage`

- `dataset_version_id`
- `run_id`
- `model_version_id`
- `eval_run_id`
- `usage_type`
- `created_at`

## Contract Amendments

Any new shared key, table, enum value, or cross-agent edge must be proposed in a checkpoint with:

1. The exact field or table name.
2. The owner agent and consumers.
3. The lineage question it enables.
4. The storage layer where it belongs.
